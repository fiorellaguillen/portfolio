---
title: "Ayabaca Street: Urban Mobility and Public Space Improvement"
categories: ["Urban Design"]
image: '/images/ayabaca1_after.png'
page-navigation: false
lightbox: true
format:
  html:
    toc: true
    page-layout: full
freeze: true
---

This project focuses on **enhancing sustainable urban mobility and revitalizing public space along Ayabaca street**, within the district of Rímac in Lima. The intervention redefines the street as a space for people rather than cars, connecting it with nearby landmarks such as the Barraganes square and the adjacent church, while reinforcing local heritage elements like old tram lines and religious routes.

The proposal promotes walkability, expands green areas, and introduces new public uses such as playgrounds, sports zones, and community gardens. Sidewalks and streets are reconfigured to respond to current mobility needs, and the public realm is upgraded with urban furniture, lighting, and vegetation.

Integrated into the broader urban and heritage projects of the PEPRR, this intervention aims to **reactivate neighborhood life, enhance cultural value, and improve the overall quality of the urban environment**.

::: caption
Project's plan
:::

![](/images/ayabaca_planta.png){fig-align="center"}

### Before and after

::::: {#slider1 .image-slider}
<img src="/images/ayabaca1_before.png"/>

::: overlay
<img src="/images/ayabaca1_after.png"/>
:::

::: slider-handle
:::
:::::

::::: {#slider2 .image-slider}
<img src="/images/ayabaca2_before.png"/>

::: overlay
<img src="/images/ayabaca2_after.png"/>
:::

::: slider-handle
:::
:::::

```{=html}
<script>
  function initImageSlider(slider) {
    const overlay = slider.querySelector('.overlay');
    const handle = slider.querySelector('.slider-handle');
    const imgBase = slider.querySelector('img');

    function syncOverlaySize() {
      const rect = imgBase.getBoundingClientRect();
      overlay.querySelector('img').style.width = rect.width + 'px';
      overlay.querySelector('img').style.height = rect.height + 'px';
    }

    function updateSlider(x) {
      const rect = slider.getBoundingClientRect();
      let pos = Math.max(0, Math.min(x - rect.left, rect.width));
      const percentage = (pos / rect.width) * 100;
      overlay.style.width = percentage + '%';
      handle.style.left = percentage + '%';
    }

    let isDragging = false;

    function startDrag(e) {
      isDragging = true;
      e.preventDefault();
    }

    function stopDrag() {
      isDragging = false;
    }

    function doDrag(e) {
      if (!isDragging) return;
      if (e.touches) {
        updateSlider(e.touches[0].clientX);
      } else {
        updateSlider(e.clientX);
      }
    }

    window.addEventListener('resize', syncOverlaySize);
    window.addEventListener('load', syncOverlaySize);

    handle.addEventListener('mousedown', startDrag);
    handle.addEventListener('touchstart', startDrag);
    window.addEventListener('mousemove', doDrag);
    window.addEventListener('touchmove', doDrag);
    window.addEventListener('mouseup', stopDrag);
    window.addEventListener('touchend', stopDrag);
  }

  // Inicializa todos los sliders automáticamente
  window.addEventListener('DOMContentLoaded', () => {
    const sliders = document.querySelectorAll('.image-slider');
    sliders.forEach(slider => initImageSlider(slider));
  });
</script>
```
